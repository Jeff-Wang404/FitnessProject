import os
from sklearn.model_selection import train_test_split
from pathlib import Path

def split_data(source_dir, target_dir, train_size=0.8, val_size=0.1):
    # identify the supported exercises

    # read the contents

    # split the data into train, val, and test
    pass